<Window x:Class="MyMarkdownEditor.Settings.BackgroundColorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="背景色の設定"
        Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="背景色を16進数で入力してください（例: #FFFFFF）"
                   TextWrapping="Wrap" Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" x:Name="ColorTextBox"
                 FontSize="14" Padding="5"
                 TextChanged="ColorTextBox_TextChanged"/>

        <TextBlock Grid.Row="3" Text="プレビュー:" Margin="0,0,0,5"/>
        <Border Grid.Row="4" x:Name="PreviewBorder"
                BorderBrush="Gray" BorderThickness="1"
                Background="White" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,0,10,0"
                    Click="OkButton_Click" IsDefault="True"/>
            <Button Content="キャンセル" Width="80"
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
